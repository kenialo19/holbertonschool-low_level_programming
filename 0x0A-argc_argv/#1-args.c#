#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * @argc: count.
 * @argvUnused: unused variable.
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argcy);
return (0);
}
