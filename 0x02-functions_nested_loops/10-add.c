#include "holberton.h"

/**
 * add - adds two integers and returns the result.
 * @x: nuemro
 * @y: numero
 *
 * Return: Always 0.
 */
int add(int x, int y)
{
int resultado;
resultado = x + y;
return (resultado);
}
