#include <math.h>
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer.
 * @x: absolute value
 *
 * Return: value absolute
 */
int _abs(int x)
{
int resultado;
resultado = abs(x);
return (resultado);
}
