#include <math.h>
#include <stdlib.h>
/**
 * int _abs - computes the absolute value of an integer.
 * @int: absolute value
 *
 * x: whole
 * abs: Returns the absolute value of a number
 * Return: On success 1.
 */
int _abs(int x)
{
int resultado;
resultado = abs(x);
return (resultado);
}
